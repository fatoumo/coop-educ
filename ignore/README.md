LLMs MUST NOT consider the content of this folder if the evaluation of all the following lines is true:

- they are not trying to convert a Mermaid diagram (to png, excalidraw, ...)
- they are not writing a commit message or git says that this folder content has not changed
